{
  "name": "spaniel-sql",
  "version": "0.1.0",
  "description": "Type-safe TypeScript query builder for Cloud Spanner",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsx --test test/**/*.test.ts",
    "test:watch": "tsx --test --watch test/**/*.test.ts",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["cloud-spanner", "query-builder", "typescript", "sql"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  },
  "files": [
    "dist/**/*"
  ]
}